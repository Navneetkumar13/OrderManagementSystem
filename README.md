# OrderManagementSystem
An order management system with the functionality of creating and modifying users, orders with REST API's
Built on (maven)Java v17, SpringBoot v3.0.2 and database used is MySql for storing and handling the data.
Dependencies include Spring Web, Spring Boot DevTools, Lombok, Spring Data JPA and MySQL Driver.
